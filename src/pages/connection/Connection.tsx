import { FC } from 'react';
import styled from 'styled-components';

export const Connection: FC = () => {
  return (
    <Wrapper>
      <h1>This is connection window</h1>
    </Wrapper>
  );
};

const Wrapper = styled.div`
  //
`;
